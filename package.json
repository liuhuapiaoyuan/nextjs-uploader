{
  "name": "nextjs-uploader",
  "version": "1.0.0",
  "description": "NextJS file uploader component", 
  "workspaces": ["packages/*","app/example"],
  "scripts": {
    "build": "tsc",
    "dev": "bun --filter '*' dev",
    "dev:core": "bun  --filter '@nextjs-uploader/core'  dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/josephluck/nextjs-uploader.git"
  },
  "keywords": [
    "nextjs",
    "uploader",
    "file",
    "upload"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}